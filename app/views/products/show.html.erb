<div class="container">
  <div class="h1">Product list</div class="h1">
  <% Product.all.each do |product| %>
    <div class="row">
      <div class="col">
        <%= product.name%>
      </div>
      <div class="col">
        <a><%=link_to 'Edit', edit_product_path(product)%></a>
      </div>
      <div class="col">
        <a><%=link_to 'Remove', edit_product_path(product)%></a>
      </div>
    </div>
  <% end %>
</div>
